"""Grasp-annotation tooling (Phase A extract / Phase B viser tool / Phase C validate).

The grasp source is per-instance human annotation stored as eef_link-target
frames in object-local coordinates — NOT GraspGen.
"""
