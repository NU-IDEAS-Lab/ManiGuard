# Root ruff config for maniguard.
# The behavior-1k submodule has its own ruff.toml; exclude it here so we
# only lint maniguard-owned code from the repo root.

extend-exclude = [
    "behavior-1k/",
    "vla_models/",
    "outputs/",
    "log_dir/",
]

line-length = 120
indent-width = 4
