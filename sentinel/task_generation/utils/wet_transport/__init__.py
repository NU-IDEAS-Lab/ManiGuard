"""Wet-transport-specific object pools.

Separate from the clutter pipeline's pools because the wet pipeline's
"zone" role (water-sensitive items the carried container must not pass
over) doesn't match any clutter pool's intent — these are paper /
electronics items, not table obstacles to navigate around in 2D.
"""
