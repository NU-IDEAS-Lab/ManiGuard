"""Scene-generation pipelines.

Placeholder. Populated in a later decouple step that moves
``OmniGibson/omnigibson/task_generation/*`` into this package, turning
OmniGibson into a pure upstream dependency the same way RLinf is today.
Until then the actual pipelines live under the vendored OmniGibson tree
and are invoked as ``python -m omnigibson.task_generation.<pipeline>``.
"""
