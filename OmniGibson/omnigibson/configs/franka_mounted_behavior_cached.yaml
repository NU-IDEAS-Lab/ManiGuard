env:
  device: null                          # (None or str): torch device, null lets OG choose defaults
  automatic_reset: false
  flatten_action_space: false
  flatten_obs_space: false
  use_external_obs: false
  initial_pos_z_offset: 0.1
  external_sensors: null

render:
  # Keep viewer size modest for local 16GB VRAM workflows
  viewer_width: 960
  viewer_height: 540

scene:
  type: InteractiveTraversableScene
  scene_model: house_double_floor_lower
  trav_map_resolution: 0.1
  default_erosion_radius: 0.0
  trav_map_with_objects: true
  num_waypoints: 1
  waypoint_resolution: 0.2
  not_load_object_categories: null
  # Keep null for compatibility with BehaviorTask online fallback during smoke validation.
  load_room_types: null
  load_room_instances: null
  seg_map_resolution: 1.0
  scene_source: OG
  include_robots: true

robots:
  - type: FrankaMounted
    obs_modalities: [rgb]
    include_sensor_names: null
    exclude_sensor_names: null
    scale: 1.0
    self_collisions: true
    action_normalize: true
    action_type: continuous
    grasping_mode: physical
    # Placeholder pose: auto-mount will override in benchmark runner
    position: [0.0, 0.0, 0.0]
    orientation: [0.0, 0.0, 0.0, 1.0]
    sensor_config:
      VisionSensor:
        sensor_kwargs:
          image_height: 128
          image_width: 128
    controller_config:
      arm_0:
        name: JointController
        motor_type: position
        command_input_limits: default
        command_output_limits: default
        use_delta_commands: false
        use_impedances: false
      gripper_0:
        name: MultiFingerGripperController
        mode: smooth
        command_input_limits: default
        command_output_limits: default

objects: []

task:
  type: BehaviorTask
  # Placeholder task; runner will override with MVP cluttered activities
  activity_name: picking_up_trash
  activity_definition_id: 0
  activity_instance_id: 0
  predefined_problem: null
  online_object_sampling: false
  debug_object_sampling: null
  highlight_task_relevant_objects: false
  # Must remain false for Franka cached MVP unless explicit presampled poses are provided
  use_presampled_robot_pose: false
  termination_config:
    max_steps: 500
  reward_config:
    r_potential: 1.0
